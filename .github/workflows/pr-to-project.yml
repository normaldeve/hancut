name: Add PR to Project

on:
  pull_request:
    types: [opened, ready_for_review]

jobs:
  add-to-project:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/add-to-project@v0.5.0
        with:
          project-url: https://github.com/users/normaldeve/projects/2
          github-token: ${{ secrets.SECRET_KEY }}